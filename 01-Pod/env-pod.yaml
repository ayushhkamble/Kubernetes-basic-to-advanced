apiVersion: v1
kind: Pod

metadata:
  name: env-pod

spec:
  containers:
  - name: app-container
    image: nginx

    env:
    - name: ENVIRONMENT
      value: "Development"

    - name: APP_NAME
      value: "MyApp"