apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: my-app   # matches BOTH stable and canary pods
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80