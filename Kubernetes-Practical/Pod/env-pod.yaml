apiVersion: v1
kind: Pod
metadata:
  name: env-pod
  labels:
    app: env
spec:
  containers:
  - name: app-container
    image: nginx:latest

    env:
    - name: ENVIRONMENT
      value: "Development"
    
    - name: APP_NAME
      value: "MyApp"
